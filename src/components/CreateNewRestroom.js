import React, { Component } from 'react';
import {CREATE_NEW_RESTROOM} from '../redux/types'
import {connect} from 'react-redux'

class CreateNewRestroom extends Component {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}

export default CreateNewRestroom;